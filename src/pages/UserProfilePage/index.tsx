import { IUserProfilePageProps } from "./types/interfaces";

import { Box } from "@mui/material";
import './index.scss';

export const UserProfilePage: React.FC<IUserProfilePageProps> = ({ }: IUserProfilePageProps): JSX.Element => {
    return <Box>
        
    </Box>
};