import { IUserPageProps } from "./types/interfaces";

import './index.scss';
import { Box } from "@mui/material";

export const UserPage: React.FC<IUserPageProps> = ({ }: IUserPageProps) => {
    return <Box>
        
    </Box>
};